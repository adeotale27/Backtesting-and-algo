# CAS Rule Expiry Automation — solo install

flask>=3.0
waitress>=3.0
pytest>=7.0

# kiteconnect is loaded from ./vendor/pykiteconnect (no pip needed).
# Optional if you prefer PyPI instead of vendor:
# kiteconnect>=4.0
